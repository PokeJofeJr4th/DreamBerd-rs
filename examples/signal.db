const const use = (val) -> {
    var var obj = {}!
    obj.call = (val)->{
        var var ret = self.value!
        if(; val ==== undefined, {self.value = val!})!
        ret
    }!
    obj.value = val!
    obj
}!

var var x = use(4)!

x(10)!

x()?

x.value?
