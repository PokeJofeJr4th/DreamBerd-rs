const const loop🤫 = (idx) -> {
    var var msg = ""!
    if(idx % 3  ==  0, {msg += "fizz"!})!
    if(idx % 5  ==  0, {msg += "buzz"!})!
    if(msg === "", {msg += idx!})!
    msg?
    loop🤫(idx + 1)!
}!

loop🤫(1)!
